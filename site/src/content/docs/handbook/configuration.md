---
title: Configuration
description: Environment variables, config commands, and output locations.
sidebar:
  order: 5
---

## Environment variables

| Variable | Purpose |
|----------|---------|
| `GITHUB_TOKEN` | GitHub PAT for remote/crawl/publish (5000 req/hr vs 60) |
| `OLLAMA_HOST` | Override Ollama endpoint (default: auto-detect) |
| `ARTIFACT_OLLAMA_MODEL` | Force a specific Ollama model |

## Config commands

Read and write config values:

```bash
artifact config get agent_name
artifact config set agent_name vera
```

## Output locations

### Per-repo output

Each `drive` writes to `.artifact/` in the target repo:

- `decision_packet.json` — the full decision with tier, formats, constraints, truth atoms
- `blueprint/` — generated by `blueprint` command
- `review.md` — generated by `review` command

### Global state

Org-level data lives at `~/.artifact/`:

- `repos/` — cached remote repo data
- `org/` — season state, ledger, bans
- `memory/` — per-repo memory entries

## Decision packet structure

The core output is a JSON decision packet:

```json
{
  "repo_name": "my-tool",
  "tier": "Fun",
  "format_candidates": ["F2_card_deck", "F9_museum_placard"],
  "constraints": ["monospace-only", "uses-failure-mode"],
  "must_include": ["one real invariant", "one failure mode", "one CLI flag"],
  "freshness_payload": {
    "weird_detail": "...",
    "recent_change": "...",
    "sharp_edge": "..."
  }
}
```

The `freshness_payload` contains three hand-picked signals from the repo's actual source code, each with `file:line` citations.
